<%= render "shared/header" %>

<h2 class="text-center">お薬一覧</h2>
<br>
<div class="text-center">
  <%= link_to '登録', new_date_hospital_path, class:"btn btn-lg btn-primary" %>
</div>
<br>
<div class='container w-50'>
  <div class="list-group">
    <% @date_hospitals.each do |date_hospital| %> 
      <%= link_to "#{date_hospital.date}／#{date_hospital.hospital_name}", 
                    date_hospital_medication_path(date_hospital, date_hospital_id: date_hospital.id),
                    class: "list-group-item list-group-item-action text-center" %>
    <% end %>  
  </div>
</div> 